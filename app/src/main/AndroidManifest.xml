<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="india.eva.com.goldenspiral" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".ui.activities.SplashActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity android:name=".ui.activities.PriceActivity"/>
        <activity android:name=".ui.activities.ContactActivity"/>
        <activity android:name=".ui.activities.ValueActivity"/>
        <activity android:name=".ui.activities.BlogActivity"/>
        <activity android:name=".ui.activities.SignupActivity"/>
        <activity android:name=".ui.activities.LoginActivity"
                  android:launchMode="singleTop"/>
        <activity android:name=".ui.activities.TutorialActivity"/>

        <activity android:name=".ui.activities.HomeActivity"
                  android:launchMode="singleTop" />




    </application>

</manifest>
